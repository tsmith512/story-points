version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm install -g gulp
    build:
      commands:
        - gulp sass
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
